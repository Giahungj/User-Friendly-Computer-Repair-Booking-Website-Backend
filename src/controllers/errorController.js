// ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
const renderNotFoundPage = async (req, res) => {
    return res.status(404).render('layouts/layout', {
        page: 'pages/misc/error404Page.ejs',
        pageTitle: 'Không tìm thấy trang',
    });
};

// ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
export default {
    renderNotFoundPage,
};
