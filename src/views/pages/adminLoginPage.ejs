
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập</title>
    <link rel="icon" type="image/jpg" href="/images/skibidi.jpg">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/app.css">
    <style>
        body {
            background: linear-gradient(to right, #007bff, #ff00fb);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        .login-container {
            width: 30em;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            opacity: 0;
            transform: translateX(6rem);
            transition: opacity 1.2s ease, transform 2s ease;
        }
        .login-container.visible {
            opacity: 1;
            transform: translateX(0);
        }
        .login-title {
            font-weight: bold;
            color: #333;
        }
        .welcome-text {
            font-size: 4.5rem;
            font-weight: bold;
            color: #ffffff;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
            text-align: center;
            margin-bottom: 3rem;
            opacity: 0;
            transform: translateX(50px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }
        .welcome-text.visible {
            opacity: 1;
            transform: translateX(0);
        }
        .btn-primary {
            border: none;
            background-color: #007bff;
        }
        .btn-primary:hover {
            background-color: #78b9ff;
            color: #004fa3;
        }
    </style>
</head>
<div>
    <h1 class="welcome-text">TechFix Cửa hàng sửa chữa thiết bị </h1>
    <div class="d-flex justify-content-center">
        <div class="login-container">
            <h2 class="text-center login-title">Admin Đăng nhập</h2>

            <% if (EC === -1 && EC) { %>
                <div class="alert alert-danger text-center"><%= EM %></div>
            <% } %>
            <% if (EC === 1 && EC) { %>
                <div class="alert alert-warning text-center"><%= EM %></div>
            <% } %>
            <form action="/admin-login/login" method="POST">
                <div class="mb-3">
                    <label for="email" class="form-label">Email:</label>
                    <input type="email" class="form-control" id="email" name="email" placeholder="Nhập email của bạn" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Mật khẩu:</label>
                    <input type="password" class="form-control" id="password" name="password" placeholder="Nhập mật khẩu" required>
                </div>
                <button type="submit" class="btn btn-primary w-100">Đăng nhập</button>
            </form>
        </div>
    </div>
</div>

<script>
    // Thêm class 'visible' để kích hoạt transition khi trang tải
    document.addEventListener('DOMContentLoaded', () => {
        const welcomeText = document.querySelector('.welcome-text');
        const loginContainer = document.querySelector('.login-container');
        setTimeout(() => {
            welcomeText.classList.add('visible');
            loginContainer.classList.add('visible');
        }, 500);
    });
</script>
</html>

